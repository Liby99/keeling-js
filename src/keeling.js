"use strict";
exports.__esModule = true;
var Keeling = function (options) {
    var config = options.config, configPath = options.configPath;
    if (config) {
    }
    else if (configPath) {
    }
    else {
        throw new Error('');
    }
};
exports["default"] = Keeling;
